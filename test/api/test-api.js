// stub for writing api tests

// GET /user/ID/

// GET /user/ID/projects/
// todo if not authenticated, cannot access
// todo if not authenticated as the user identified by ID (and not admin), cannot access
// todo if authenticated as the user identified by ID, can access
// todo if authenticated as an admin user, can access

// GET /users/